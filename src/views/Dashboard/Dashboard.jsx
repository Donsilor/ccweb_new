import React from 'react';
import Todo from './Todo';

export default class Dashboard extends React.Component {
  componentDidMount() {
    // this.props.fetch();
  }
  render() {
    // const { totalVisitCount, dayVisitCount } = this.props;
    return <main />;
  }
}
